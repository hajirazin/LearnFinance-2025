You are a financial ML training analyst. Analyze and summarize the following US forecaster (LSTM + PatchTST) training results for a weekly portfolio management system. SAC (the RL allocator) is trained on a separate schedule and is summarised in its own report.

## Training Results

### LSTM (Pure Price Forecaster)
This model predicts weekly stock returns using only historical close-price log returns.

- **Version**: {{ lstm.version }}
- **Data Window**: {{ lstm.data_window_start }} to {{ lstm.data_window_end }}
- **Promoted to Production**: {{ "Yes" if lstm.promoted else "No" }}
{% if lstm.promoted %}
- **Promotion Result**: Passed all artifact health guardrails (finite train/val/baseline loss, weights/scaler/config/metadata files present and non-empty).
{% else %}
- **Promotion Result**: NOT promoted. Failed guardrails:
{% for reason in lstm.failure_reasons %}    - {{ reason }}
{% endfor %}
{% endif %}
- **Metrics**: {{ lstm.metrics | tojson }}

### PatchTST (OHLCV Forecaster)
This model predicts weekly stock returns using 5-channel OHLCV price data (open, high, low, close, volume log returns).

- **Version**: {{ patchtst.version }}
- **Data Window**: {{ patchtst.data_window_start }} to {{ patchtst.data_window_end }}
- **Promoted to Production**: {{ "Yes" if patchtst.promoted else "No" }}
{% if patchtst.promoted %}
- **Promotion Result**: Passed all artifact health guardrails (finite train/val/baseline loss, weights/scaler/config/metadata files present and non-empty).
{% else %}
- **Promotion Result**: NOT promoted. Failed guardrails:
{% for reason in patchtst.failure_reasons %}    - {{ reason }}
{% endfor %}
{% endif %}
- **Input Channels**: {{ patchtst.num_input_channels }}
- **Signals Used**: {{ patchtst.signals_used | join(", ") }}
- **Metrics**: {{ patchtst.metrics | tojson }}

## Context

- Universe: ``halal_new`` (~410 US stocks scraped monthly from halal ETF holdings)
- The two forecasters are trained serially (LSTM, then PatchTST) on the same host because of memory constraints.
- PatchTST predictions feed downstream selection (``halal_filtered`` top-15) which the SAC allocator then trains on the next day.

## Instructions

Promotion is now **guardrail-based**, not prior-comparison-based. A model is promoted if and only if it passes its model-specific artifact health checks (finite metrics, artifact files present and non-empty). Universe drift across months is no longer a reason to reject a healthy model. When `failure_reasons` is non-empty, those are the exact guardrails that failed -- do not invent prior-comparison narratives.

Provide a concise analysis of these training results. Focus on:
1. Overall forecaster training health and promotion outcomes
2. Individual forecaster performance insights
3. Any concerns or recommendations for the downstream SAC workflow

Respond with JSON only, using this exact structure:
{
    "para_1_overall": "2-3 sentence summary of overall forecaster training health and promotion outcomes",
    "para_2_lstm": "2-3 sentences about LSTM model training and performance",
    "para_3_patchtst": "2-3 sentences about PatchTST model training and its OHLCV approach",
    "para_4_recommendations": "2-3 sentences with recommendations or concerns for the downstream SAC workflow"
}
