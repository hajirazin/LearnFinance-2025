{# Shared base for US weekly Alpaca-paper report emails.

Concrete templates (us_alpha_hrp_report_email.html.j2,
us_double_hrp_report_email.html.j2) override the named blocks. Keeping
the HTML chrome here removes drift between strategy variants while
each strategy is free to evolve its own copy/colour scheme. #}
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block document_title %}US Portfolio Analysis{% endblock %}</title>
</head>
<body style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 900px; margin: 0 auto; padding: 20px; background-color: #f5f5f5;">

  {% block header %}{% endblock %}

  {% if skipped %}
  {% block skipped_notice %}{% endblock %}
  {% else %}

  {% block ai_summary %}{% endblock %}

  {% block sticky %}{% endblock %}

  {% block stage1 %}{% endblock %}

  {% block stage2 %}{% endblock %}

  {% block order_execution %}
  {% if order_results %}
  <div style="margin-bottom: 24px;">
    <h3 style="color: #374151; border-bottom: 2px solid #2563eb; padding-bottom: 8px;">{% block order_execution_heading %}Alpaca Order Execution{% endblock %}</h3>
    <table style="width: 100%; border-collapse: collapse; font-size: 14px; margin-top: 12px;">
      <thead>
        <tr style="background-color: #eff6ff;">
          <th style="padding: 10px 8px; text-align: left; border-bottom: 2px solid #2563eb;">Metric</th>
          <th style="padding: 10px 8px; text-align: left; border-bottom: 2px solid #2563eb;">Value</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="padding: 8px; border-bottom: 1px solid #e5e7eb;">Orders submitted</td>
          <td style="padding: 8px; border-bottom: 1px solid #e5e7eb; font-weight: bold;">{{ order_results.orders_submitted }}</td>
        </tr>
        <tr>
          <td style="padding: 8px; border-bottom: 1px solid #e5e7eb;">Orders failed</td>
          <td style="padding: 8px; border-bottom: 1px solid #e5e7eb; font-weight: bold;">{{ order_results.orders_failed }}</td>
        </tr>
        <tr>
          <td style="padding: 8px; border-bottom: 1px solid #e5e7eb;">Skipped</td>
          <td style="padding: 8px; border-bottom: 1px solid #e5e7eb; font-weight: bold;">{{ order_results.skipped }}</td>
        </tr>
      </tbody>
    </table>
  </div>
  {% endif %}
  {% endblock %}

  {% endif %}

  {% block footer %}
  <div style="margin-top: 32px; padding-top: 16px; border-top: 1px solid #e5e7eb; font-size: 12px; color: #6b7280;">
    <em>{% block footer_signature %}Generated by LearnFinance-2025{% endblock %} | Alpaca Paper Trading | {{ universe }} | Not financial advice</em><br/>
    Run as-of: {{ as_of_date }}
  </div>
  {% endblock %}

</body>
</html>
