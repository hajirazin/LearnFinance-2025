.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.git/

# Runtime data lives on the host via a bind mount (see repo-root
# docker-compose.yml `brain-api.volumes`). Models flow Mac -> HF -> Pi
# via STORAGE_BACKEND=hf_first; do not bake data/ into the image.
data/
