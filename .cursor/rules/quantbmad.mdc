---
description: QuantBMAD peers and optional party for LearnFinance-2025 research/math work
alwaysApply: true
---

# QuantBMAD

- Quant peers live as skills `qb-agent-*` (researcher, pm, ml, risk, skeptic, validation, dev, architect). Invoke them **only when the user asks** — do not auto-select a plan/implement workflow.
- **Party `quantbmad`:** equal peers; no boss except Razin. Invoke only when the user asks: `bmad-party-mode --party quantbmad --mode subagent` (unanimous Agree or 3 loops → ask Razin).
- Research-glob paths are listed in `_bmad/quantbmad/research_globs.py`. Prefer evidence + Skeptic cert for math/model ships when the user wants that rigor; never forge `evidence-cert.md` PASS (`qb-agent-skeptic` only).
- Never write `Status: Approved` into `docs/plans/**/plan.md` (human only).
- Optional helpers: `check_repo_green.py`, `validate_party_consensus.py`, `search_ledger.py`. Do not use `git commit --no-verify` for Research-glob work when the research gate is enabled.
