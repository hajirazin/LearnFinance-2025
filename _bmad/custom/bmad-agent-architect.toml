# QuantBMAD: AGENTS.md invariants + storage/orchestration context for Architect.
[agent]
persistent_facts = [
  "file:{project-root}/AGENTS.md",
  "Math correctness > DRY — never break math to simplify code.",
  "No silent fallbacks anywhere.",
  "Paper trading is default; live is per-account env-driven opt-in only.",
  "Sticky-selection partitions must be unique; never conflate weight-band and rank-band.",
  "Model buckets are isolated — promoting one must never touch another's current pointer.",
  "India symbols carry .NS suffix end-to-end; never strip it.",
  "No training schedules on Monday (inference-only day).",
  "Use storage abstraction (local/HF); endpoints stay stateless.",
  "Temporal orchestrates; brain_api owns business logic — do not put math in Temporal activities.",
  "Research-glob / architecture changes that touch math go through QuantBMAD qb-plan first.",
]
